package dev.vernonlim.cw2024game.elements.actors;

import dev.vernonlim.cw2024game.configs.FighterPlaneConfig;

/**
 * An Enemy Plane.
 */
public class EnemyPlane extends FighterPlane {
    /**
     * Constructs an Enemy Plane with the given params.
     *
     * @param config the configuration object containing the necessary data to construct the EnemyPlane
     */
    public EnemyPlane(FighterPlaneConfig config) {
        super(config);
    }
}
